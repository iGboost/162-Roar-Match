platform :ios, '15.0'
inhibit_all_warnings!

target 'Roar Match' do
  # динамические фреймворки:
  # use_frameworks!
  # чаще стабильнее статическая линковка:
  use_frameworks! :linkage => :static

  # use_modular_headers!  # включай только если реально нужно (реже требуется)

  # Примеры актуальных зависимостей Firebase:
  # pod 'Firebase/Core'            # ❌ устарело — не используем
  # Вместо этого:
  # pod 'FirebaseAnalytics'
  # pod 'FirebaseAuth'
  # pod 'FirebaseMessaging'
  # pod 'FirebaseCrashlytics'

  # Добавь свои поды тут…
end